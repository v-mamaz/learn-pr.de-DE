## <a name="motivation"></a><span data-ttu-id="eaa45-101">Motivation</span><span class="sxs-lookup"><span data-stu-id="eaa45-101">Motivation</span></span>
<span data-ttu-id="eaa45-102">Azure PowerShell können Sie die Befehle schreiben, und führen Sie sie.</span><span class="sxs-lookup"><span data-stu-id="eaa45-102">Azure PowerShell lets you write commands and execute them immediately.</span></span> <span data-ttu-id="eaa45-103">Dies bezeichnet man als **im interaktiven Modus**.</span><span class="sxs-lookup"><span data-stu-id="eaa45-103">This is known as **interactive mode**.</span></span>

<span data-ttu-id="eaa45-104">Denken Sie daran, dass das Ziel im Beispiel (Customer Relationship Management, CRM) besteht darin, drei Test-Umgebungen mit virtuellen Computern zu erstellen.</span><span class="sxs-lookup"><span data-stu-id="eaa45-104">Recall that the overall goal in the Customer Relationship Management (CRM) example is to create three test environments containing VMs.</span></span> <span data-ttu-id="eaa45-105">Sie können Ressourcengruppen, um sicherzustellen, dass die virtuellen Computer werden in separaten Umgebungen organisiert: eins für Komponententests bereit, eine für Integrationstests und für Tests der Benutzerakzeptanz.</span><span class="sxs-lookup"><span data-stu-id="eaa45-105">You will use resource groups to ensure the VMs are organized into separate environments: one for unit testing, one for integration testing, and one for acceptance testing.</span></span> <span data-ttu-id="eaa45-106">Sie müssen nur einmal für die Ressourcengruppen zu erstellen, d. h., die im interaktiven Modus von PowerShell eine gute Wahl ist.</span><span class="sxs-lookup"><span data-stu-id="eaa45-106">You only need to create the resource groups once, which means using the interactive mode of PowerShell is a good choice.</span></span>

<span data-ttu-id="eaa45-107">In diesem Abschnitt erfahren Sie, wie Sie mithilfe von PowerShell interaktiv anmelden beim Azure-Abonnement, und erstellen Sie Ressourcengruppen.</span><span class="sxs-lookup"><span data-stu-id="eaa45-107">This section shows you how to use PowerShell interactively to login to your Azure subscription and create resource groups.</span></span>

## <a name="what-are-powershell-cmdlets"></a><span data-ttu-id="eaa45-108">Welche PowerShell-Cmdlets sind?</span><span class="sxs-lookup"><span data-stu-id="eaa45-108">What are PowerShell cmdlets?</span></span>
<span data-ttu-id="eaa45-109">Ein PowerShell-Befehl aufgerufen wird eine **Cmdlet** (ausgesprochen "Command-Let").</span><span class="sxs-lookup"><span data-stu-id="eaa45-109">A PowerShell command is called a **cmdlet** (pronounced "command-let").</span></span> <span data-ttu-id="eaa45-110">Ein Cmdlet ist ein Befehl, der eine einzelne Funktion bearbeitet.</span><span class="sxs-lookup"><span data-stu-id="eaa45-110">A cmdlet is a command that manipulates a single feature.</span></span> <span data-ttu-id="eaa45-111">Der Begriff **Cmdlet** richtet sich an "kleine Befehl" impliziert.</span><span class="sxs-lookup"><span data-stu-id="eaa45-111">The term **cmdlet** is intended to imply "small command".</span></span> <span data-ttu-id="eaa45-112">Gemäß der Konvention werden Cmdlet-Autoren wird empfohlen, die Cmdlets auf einfache und einzelnen Zweck zu halten.</span><span class="sxs-lookup"><span data-stu-id="eaa45-112">By convention, cmdlet authors are encouraged to keep cmdlets simple and single purpose.</span></span>

<span data-ttu-id="eaa45-113">Das PowerShell-Basisprodukt Lieferumfang von Cmdlets, die Arbeit mit Funktionen wie Sitzungen und Hintergrundaufträge ab.</span><span class="sxs-lookup"><span data-stu-id="eaa45-113">The base PowerShell product ships with cmdlets that work with features such as sessions and background jobs.</span></span> <span data-ttu-id="eaa45-114">Die PowerShell-Installation um Cmdlets zu erhalten, die andere Funktionen ändern hinzugefügt Module.</span><span class="sxs-lookup"><span data-stu-id="eaa45-114">You add modules to your PowerShell installation to get cmdlets that manipulate other features.</span></span> <span data-ttu-id="eaa45-115">Es gibt z. B. Drittanbieter-Module zum Arbeiten mit ftp, verwalten Ihr Betriebssystem, das Zugriff auf das Dateisystem usw.</span><span class="sxs-lookup"><span data-stu-id="eaa45-115">For example, there are third-party modules to work with ftp, administer your operating system, access the file system, etc.</span></span>

<span data-ttu-id="eaa45-116">Cmdlets folgen eine Verb-Substantiv-Namenskonvention gilt. z. B. **Get-Process**, **Format-Table**, und **Start-Service**.</span><span class="sxs-lookup"><span data-stu-id="eaa45-116">Cmdlets follow a verb-noun naming convention; for example, **Get-Process**, **Format-Table**, and **Start-Service**.</span></span> <span data-ttu-id="eaa45-117">Es gibt auch eine Konvention für das Verb Wahl: "get", um Daten abzurufen, "set" zum Einfügen oder Aktualisieren von Daten, "formatieren" für die direkte Ausgabe an ein Ziel usw. zum Formatieren von Daten "out".</span><span class="sxs-lookup"><span data-stu-id="eaa45-117">There is also a convention for verb choice: "get" to retrieve data, "set" to insert or update data, "format" to format data, "out" to direct output to a destination, etc.</span></span>

<span data-ttu-id="eaa45-118">Cmdlet-Autoren werden empfohlen, eine Hilfedatei für die einzelnen Cmdlets enthalten.</span><span class="sxs-lookup"><span data-stu-id="eaa45-118">Cmdlet authors are encouraged to include a help file for each cmdlet.</span></span> <span data-ttu-id="eaa45-119">Die **Get-Help** Cmdlet zeigt an, der die Hilfedatei für ein Cmdlet:</span><span class="sxs-lookup"><span data-stu-id="eaa45-119">The **Get-Help** cmdlet displays the help file for any cmdlet:</span></span>

```
Get-Help <cmdlet-name> -detailed
```

## <a name="what-is-azurerm"></a><span data-ttu-id="eaa45-120">Was ist AzureRM?</span><span class="sxs-lookup"><span data-stu-id="eaa45-120">What is AzureRM?</span></span>
<span data-ttu-id="eaa45-121">**AzureRM** der formelle Name für das Azure PowerShell-Modul enthält Cmdlets zum Arbeiten mit Azure-Features (die **RM** im Namen steht für **Resource Manager**).</span><span class="sxs-lookup"><span data-stu-id="eaa45-121">**AzureRM** is the formal name for the Azure PowerShell module containing cmdlets to work with Azure features (the **RM** in the name stands for **Resource Manager**).</span></span> <span data-ttu-id="eaa45-122">Es enthält Hunderte von Cmdlets, mit denen Sie fast jeden Aspekt aller Azure-Ressourcen zu steuern.</span><span class="sxs-lookup"><span data-stu-id="eaa45-122">It contains hundreds of cmdlets that let you control nearly every aspect of every Azure resource.</span></span> <span data-ttu-id="eaa45-123">Arbeiten mit Ressourcengruppen, Speicher, virtuelle Computer können Sie Azure Active Directory, Container, Machine Learning.</span><span class="sxs-lookup"><span data-stu-id="eaa45-123">You can work with resource groups, storage, virtual machines, Azure Active Directory, containers, machine learning, etc.</span></span>

## <a name="how-to-create-a-resource-group"></a><span data-ttu-id="eaa45-124">Gewusst wie: Erstellen einer Ressourcengruppe</span><span class="sxs-lookup"><span data-stu-id="eaa45-124">How to create a resource group</span></span>
<span data-ttu-id="eaa45-125">Als Nächstes erstellen wir eine Ressourcengruppe mit einer lokalen Installation von Azure PowerShell.</span><span class="sxs-lookup"><span data-stu-id="eaa45-125">Next we'll create a resource group using a local installation of Azure PowerShell.</span></span> 

<span data-ttu-id="eaa45-126">Es sind vier Schritte notwendig:</span><span class="sxs-lookup"><span data-stu-id="eaa45-126">There are four steps:</span></span> 
1. <span data-ttu-id="eaa45-127">Importieren der Azure-cmdlets</span><span class="sxs-lookup"><span data-stu-id="eaa45-127">Import the Azure cmdlets</span></span>
1. <span data-ttu-id="eaa45-128">Verbinden mit Ihrem Azure-Abonnement</span><span class="sxs-lookup"><span data-stu-id="eaa45-128">Connect to your Azure subscription</span></span>
1. <span data-ttu-id="eaa45-129">Ressourcengruppe erstellen</span><span class="sxs-lookup"><span data-stu-id="eaa45-129">Create the resource group</span></span>
1. <span data-ttu-id="eaa45-130">Stellen Sie sicher, dass die Erstellung erfolgreich (siehe unten) wurde.</span><span class="sxs-lookup"><span data-stu-id="eaa45-130">Verify that creation was successful (see below).</span></span>

![Schritte zum Erstellen einer Ressource in Azure mithilfe von Azure PowerShell](../images/5-MOCKUP-create-resource-overview.png)

<span data-ttu-id="eaa45-132">Jeder Schritt entspricht einem anderen Cmdlet.</span><span class="sxs-lookup"><span data-stu-id="eaa45-132">Each step corresponds to a different cmdlet.</span></span>

### <a name="import"></a><span data-ttu-id="eaa45-133">Importieren</span><span class="sxs-lookup"><span data-stu-id="eaa45-133">Import</span></span>
<span data-ttu-id="eaa45-134">Beim Start lädt PowerShell nur die Kern-Cmdlets in der Standardeinstellung an.</span><span class="sxs-lookup"><span data-stu-id="eaa45-134">At startup, PowerShell loads only the core cmdlets by default.</span></span> <span data-ttu-id="eaa45-135">Dies bedeutet, dass die Cmdlets müssen Sie für die Arbeit mit Azure wird nicht geladen werden.</span><span class="sxs-lookup"><span data-stu-id="eaa45-135">This means the cmdlets you need to work with Azure won't be loaded.</span></span> <span data-ttu-id="eaa45-136">Die zuverlässigste Möglichkeit, um die Cmdlets zu laden, die Sie benötigen, ist am Anfang der PowerShell-Sitzung manuell importieren.</span><span class="sxs-lookup"><span data-stu-id="eaa45-136">The most reliable way to load the cmdlets you need is to import them manually at the start of your PowerShell session.</span></span>

<span data-ttu-id="eaa45-137">Sie verwenden die **Import-Module** Cmdlet-Module laden.</span><span class="sxs-lookup"><span data-stu-id="eaa45-137">You use the **Import-Module** cmdlet to load modules.</span></span> <span data-ttu-id="eaa45-138">Dieses Cmdlet verfügt über viele Parameter, um eine Vielzahl von Situationen zu behandeln.</span><span class="sxs-lookup"><span data-stu-id="eaa45-138">This cmdlet has many parameters to handle a variety of situations.</span></span> <span data-ttu-id="eaa45-139">Beispielsweise können sie laden mehrere Module, ein bestimmtes Modulversion, die Teil eines Moduls usw. Um ein Modul während des gesamten Entwicklungsprozesses zu laden, ist einfach die Syntax auf:</span><span class="sxs-lookup"><span data-stu-id="eaa45-139">For example, it can load multiple modules, a specific module version, part of a module, etc. To load the entirety of one module, the syntax is simply:</span></span>

```powershell
Import-Module <module-name>
```

> [!TIP] <span data-ttu-id="eaa45-140">Wenn Sie feststellen, dass Sie häufig mit Azure PowerShell arbeiten, gibt es zwei Möglichkeiten, die Sie das Laden von Modulen automatisieren können.</span><span class="sxs-lookup"><span data-stu-id="eaa45-140">If you find that you work with Azure PowerShell frequently, there are two ways you can automate the module-loading process.</span></span> <span data-ttu-id="eaa45-141">Sie können einen Eintrag hinzufügen, um das PowerShell-Profil importieren das Azure-Modul beim Start aus, oder verwenden die neuesten Versionen von PowerShell das enthaltende Modul automatisch geladen, wenn Sie ein Cmdlet verwenden.</span><span class="sxs-lookup"><span data-stu-id="eaa45-141">You can add an entry to your PowerShell profile to import the Azure module at startup or use the latest versions of PowerShell which loads the containing module automatically when you use a cmdlet.</span></span>

### <a name="connect"></a><span data-ttu-id="eaa45-142">Verbinden</span><span class="sxs-lookup"><span data-stu-id="eaa45-142">Connect</span></span>
<span data-ttu-id="eaa45-143">Da Sie mit einer lokalen Installation von Azure PowerShell arbeiten, müssen Sie sich authentifizieren, bevor Sie Azure-Befehle ausführen können.</span><span class="sxs-lookup"><span data-stu-id="eaa45-143">Since you are working with a local install of Azure PowerShell, you will need to authenticate before you can execute Azure commands.</span></span> <span data-ttu-id="eaa45-144">Die **Connect-AzureRmAccount** -Cmdlet für Ihre Azure-Anmeldeinformationen aufgefordert werden, und klicken Sie dann eine Verbindung mit Ihrem Azure-Abonnement her.</span><span class="sxs-lookup"><span data-stu-id="eaa45-144">The **Connect-AzureRmAccount** cmdlet prompts for your Azure credentials and then connects to your Azure subscription.</span></span> <span data-ttu-id="eaa45-145">Es weist viele optionale Parameter, jedoch ist alles, was Sie benötigen eine interaktive Eingabeaufforderung, klicken Sie dann keine Parameter benötigt werden:</span><span class="sxs-lookup"><span data-stu-id="eaa45-145">It has many optional parameters, but if all you need is an interactive prompt, then no parameters are needed:</span></span>

```powershell
Connect-AzureRmAccount
```

### <a name="create"></a><span data-ttu-id="eaa45-146">Erstellen</span><span class="sxs-lookup"><span data-stu-id="eaa45-146">Create</span></span>
<span data-ttu-id="eaa45-147">Die **New-AzureRmResourceGroup** -Cmdlet erstellt eine Ressourcengruppe aus.</span><span class="sxs-lookup"><span data-stu-id="eaa45-147">The **New-AzureRmResourceGroup** cmdlet creates a resource group.</span></span> <span data-ttu-id="eaa45-148">Sie müssen einen Namen und Speicherort angeben.</span><span class="sxs-lookup"><span data-stu-id="eaa45-148">You must specify a name and location.</span></span> <span data-ttu-id="eaa45-149">Der Name muss innerhalb Ihres Abonnements eindeutig sein.</span><span class="sxs-lookup"><span data-stu-id="eaa45-149">The name must be unique within your subscription.</span></span> <span data-ttu-id="eaa45-150">Die Position bestimmt, die Metadaten für Ihre Ressourcengruppe gespeichert wird (der Compliance-Gründen wichtig sein kann).</span><span class="sxs-lookup"><span data-stu-id="eaa45-150">The location determines where the metadata for your resource group will be stored (which may be important to you for compliance reasons).</span></span> <span data-ttu-id="eaa45-151">Sie verwenden Zeichenfolgen wie "USA, Westen", "Europa, Norden" und "Indien (Westen)", um den Speicherort anzugeben.</span><span class="sxs-lookup"><span data-stu-id="eaa45-151">You use strings like "West US", "North Europe", or "West India" to specify the location.</span></span> <span data-ttu-id="eaa45-152">Wie bei den meisten Azure-Cmdlets, **New-AzureRmResourceGroup** weist viele optionale Parameter ist jedoch der grundlegenden Syntax:</span><span class="sxs-lookup"><span data-stu-id="eaa45-152">As with most of the Azure cmdlets, **New-AzureRmResourceGroup** has many optional parameters; however, the core syntax is:</span></span>

```powershell
New-AzureRmResourceGroup -Name <name> -Location <location>
```

### <a name="verify"></a><span data-ttu-id="eaa45-153">Überprüfen</span><span class="sxs-lookup"><span data-stu-id="eaa45-153">Verify</span></span>
<span data-ttu-id="eaa45-154">Die **Get-AzureRmResource** Listet Ihre Azure-Ressourcen.</span><span class="sxs-lookup"><span data-stu-id="eaa45-154">The **Get-AzureRmResource** lists your Azure resources.</span></span> <span data-ttu-id="eaa45-155">Dies ist hier nützlich zu überprüfen, ob die Erstellung der Ressourcengruppe erfolgreich war.</span><span class="sxs-lookup"><span data-stu-id="eaa45-155">This is useful here to verify whether creation of the resource group was successful.</span></span>

```powershell
Get-AzureRmResource
```

<span data-ttu-id="eaa45-156">Um eine präzisere Ansicht zu erhalten, können Sie die Ausgabe senden die **Get-AzureRmResource** auf die **Format-Table** Cmdlet mithilfe einer Pipe "|".</span><span class="sxs-lookup"><span data-stu-id="eaa45-156">To get a more concise view, you can send the output from the **Get-AzureRmResource** to the **Format-Table** cmdlet using a pipe '|'.</span></span>

```powershell
Get-AzureRmResource | Format-Table
```

## <a name="summary"></a><span data-ttu-id="eaa45-157">Zusammenfassung</span><span class="sxs-lookup"><span data-stu-id="eaa45-157">Summary</span></span>
<span data-ttu-id="eaa45-158">PowerShell interaktive Modus ist für einmalige Aufgaben geeignet.</span><span class="sxs-lookup"><span data-stu-id="eaa45-158">PowerShell's interactive mode is appropriate for one-off tasks.</span></span> <span data-ttu-id="eaa45-159">In unserem Beispiel verwenden wir die gleiche Ressourcengruppe für die Lebensdauer des Projekts Dies bedeutet, dass es interaktiven erstellen angemessen ist.</span><span class="sxs-lookup"><span data-stu-id="eaa45-159">In our example, we'll use the same resource group for the lifetime of the project which means creating it interactively is reasonable.</span></span> <span data-ttu-id="eaa45-160">Im interaktiver Modus ist häufig schneller und einfacher für diese Aufgabe als ein Skript zu schreiben und das Skript genau einmal ausgeführt.</span><span class="sxs-lookup"><span data-stu-id="eaa45-160">Interactive mode is often quicker and easier for this task than writing a script and executing that script exactly once.</span></span>