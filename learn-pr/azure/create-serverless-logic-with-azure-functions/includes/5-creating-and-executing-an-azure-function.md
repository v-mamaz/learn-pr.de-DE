<span data-ttu-id="525d4-101">Nachdem Sie die Funktions-App erstellt haben, erfahren Sie jetzt, wie Sie eine Azure-Funktion erstellen, konfigurieren und ausführen.</span><span class="sxs-lookup"><span data-stu-id="525d4-101">Now that we have a function app created, we'll learn how to build, configure, and execute an Azure function.</span></span>

### <a name="triggers"></a><span data-ttu-id="525d4-102">Trigger</span><span class="sxs-lookup"><span data-stu-id="525d4-102">Triggers</span></span>

<span data-ttu-id="525d4-103">Funktionen sind ereignisgesteuert, d.h., sie werden als Reaktion auf ein Ereignis ausgeführt.</span><span class="sxs-lookup"><span data-stu-id="525d4-103">Functions are event driven, which means they run in response to an event.</span></span>

<span data-ttu-id="525d4-104">Die Art Ereignis, die die Funktion startet, wird als *Trigger* bezeichnet.</span><span class="sxs-lookup"><span data-stu-id="525d4-104">The type of event that starts the function is called a *trigger*.</span></span> <span data-ttu-id="525d4-105">Sie konfigurieren eine Funktion mit genau einem Trigger.</span><span class="sxs-lookup"><span data-stu-id="525d4-105">You configure a function with exactly one trigger.</span></span>

 <span data-ttu-id="525d4-106">Azure unterstützt Trigger für die folgenden Dienste.</span><span class="sxs-lookup"><span data-stu-id="525d4-106">Azure supports triggers for the following services.</span></span>


|<span data-ttu-id="525d4-107">Dienst</span><span class="sxs-lookup"><span data-stu-id="525d4-107">Service</span></span>  |<span data-ttu-id="525d4-108">Beschreibung des Triggers</span><span class="sxs-lookup"><span data-stu-id="525d4-108">Trigger description</span></span>  |
|---------|---------|
|<span data-ttu-id="525d4-109">Blob-Speicher</span><span class="sxs-lookup"><span data-stu-id="525d4-109">Blob storage</span></span>     |  <span data-ttu-id="525d4-110">Startet eine Funktion, wenn ein neues oder aktualisiertes Blob erkannt wird.</span><span class="sxs-lookup"><span data-stu-id="525d4-110">Start a function when a new or updated blob is detected.</span></span>       |
|<span data-ttu-id="525d4-111">Cosmos DB</span><span class="sxs-lookup"><span data-stu-id="525d4-111">Cosmos DB</span></span>     |  <span data-ttu-id="525d4-112">Startet eine Funktion, wenn Einfügungen und Aktualisierungen erkannt werden.</span><span class="sxs-lookup"><span data-stu-id="525d4-112">Start a function when inserts and updates are detected.</span></span>      |
|<span data-ttu-id="525d4-113">Event Grid</span><span class="sxs-lookup"><span data-stu-id="525d4-113">Event Grid</span></span>     |   <span data-ttu-id="525d4-114">Startet eine Funktion, wenn ein Ereignis von Event Grid empfangen wird.</span><span class="sxs-lookup"><span data-stu-id="525d4-114">Start a function when an event is received from Event Grid.</span></span>       |
|<span data-ttu-id="525d4-115">HTTP</span><span class="sxs-lookup"><span data-stu-id="525d4-115">HTTP</span></span>     |   <span data-ttu-id="525d4-116">Startet eine Funktion mit einer HTTP-Anforderung.</span><span class="sxs-lookup"><span data-stu-id="525d4-116">Start a function with an HTTP request.</span></span>      |
|<span data-ttu-id="525d4-117">Microsoft Graph-Ereignisse</span><span class="sxs-lookup"><span data-stu-id="525d4-117">Microsoft Graph Events</span></span>     |  <span data-ttu-id="525d4-118">Startet eine Funktion als Reaktion auf einen von Microsoft Graph eingehenden Webhook.</span><span class="sxs-lookup"><span data-stu-id="525d4-118">Start a function in response to an incoming webhook from the Microsoft Graph.</span></span> <span data-ttu-id="525d4-119">Jede Instanz dieses Triggers kann auf einen Microsoft Graph-Ressourcentyp reagieren.</span><span class="sxs-lookup"><span data-stu-id="525d4-119">Each instance of this trigger can react to one Microsoft Graph resource type.</span></span>       |
|<span data-ttu-id="525d4-120">Queue Storage</span><span class="sxs-lookup"><span data-stu-id="525d4-120">Queue storage</span></span>     |    <span data-ttu-id="525d4-121">Startet eine Funktion, wenn ein neues Element in einer Warteschlange eingeht.</span><span class="sxs-lookup"><span data-stu-id="525d4-121">Start a function when a new item is received on a queue.</span></span> <span data-ttu-id="525d4-122">Die Warteschlangennachricht wird als Eingabe für die Funktion bereitgestellt.</span><span class="sxs-lookup"><span data-stu-id="525d4-122">The queue message is provided as input to the function.</span></span>      |
|<span data-ttu-id="525d4-123">Service Bus</span><span class="sxs-lookup"><span data-stu-id="525d4-123">Service Bus</span></span>     |  <span data-ttu-id="525d4-124">Startet eine Funktion als Reaktion auf Nachrichten aus einer Service Bus-Warteschlange.</span><span class="sxs-lookup"><span data-stu-id="525d4-124">Start a function in response to messages from a Service Bus queue.</span></span>       |
|<span data-ttu-id="525d4-125">Timer</span><span class="sxs-lookup"><span data-stu-id="525d4-125">Timer</span></span>     |  <span data-ttu-id="525d4-126">Startet eine Funktion gemäß einem Zeitplan.</span><span class="sxs-lookup"><span data-stu-id="525d4-126">Start a function on a schedule.</span></span>       |
|<span data-ttu-id="525d4-127">Webhooks</span><span class="sxs-lookup"><span data-stu-id="525d4-127">Webhooks</span></span>     |  <span data-ttu-id="525d4-128">Startet eine Funktion mit einer Webhookanforderung.</span><span class="sxs-lookup"><span data-stu-id="525d4-128">Start a function with a webhook request.</span></span>       |

### <a name="bindings"></a><span data-ttu-id="525d4-129">Bindungen</span><span class="sxs-lookup"><span data-stu-id="525d4-129">Bindings</span></span>

<span data-ttu-id="525d4-130">Azure Functions-Bindungen sind eine deklarative Möglichkeit, Daten und Dienste mit Ihrer Funktion zu verbinden.</span><span class="sxs-lookup"><span data-stu-id="525d4-130">Azure Functions bindings are a declarative way to connect data and services to your function.</span></span> <span data-ttu-id="525d4-131">Sie müssen keinen Code in Ihrer Funktion schreiben, um sich mit Datenquellen zu verbinden und Verbindungen zu verwalten.</span><span class="sxs-lookup"><span data-stu-id="525d4-131">You don't have to write code in your function to connect to data sources and manage connections.</span></span> <span data-ttu-id="525d4-132">Die Plattform übernimmt diese komplexe Aufgabe für Sie.</span><span class="sxs-lookup"><span data-stu-id="525d4-132">The platform takes care that complexity for you.</span></span> <span data-ttu-id="525d4-133">Eine Bindung hat eine Richtung.</span><span class="sxs-lookup"><span data-stu-id="525d4-133">A binding has a direction.</span></span> <span data-ttu-id="525d4-134">Ihr Code liest Daten aus *Eingabe*bindungen und schreibt Daten in *Ausgabe*bindungen.</span><span class="sxs-lookup"><span data-stu-id="525d4-134">Your code reads data from *input* bindings and writes data to *output* bindings.</span></span> <span data-ttu-id="525d4-135">Schauen wir uns ein Beispiel an.</span><span class="sxs-lookup"><span data-stu-id="525d4-135">Let's look at an example.</span></span>

<span data-ttu-id="525d4-136">Um Daten aus dem Blob-Speicher zu lesen, konfigurieren Sie eine Eingabebindung des Typs *Blob*.</span><span class="sxs-lookup"><span data-stu-id="525d4-136">To read data from Blob storage, you configure an input binding of type *blob*.</span></span> <span data-ttu-id="525d4-137">Um eine Nachricht an den Warteschlangenspeicher zu schreiben, konfigurieren Sie eine Ausgabebindung des Typs *Warteschlange*.</span><span class="sxs-lookup"><span data-stu-id="525d4-137">To write a message to Queue storage, you configure an output binding of type *queue*.</span></span>

<span data-ttu-id="525d4-138">Weitere Informationen zu den unterstützten Bindungen und Triggern finden Sie in der [Dokumentation zu Azure](https://docs.microsoft.com/azure/azure-functions/functions-triggers-bindings#supported-bindings).</span><span class="sxs-lookup"><span data-stu-id="525d4-138">For more information about the supported bindings and triggers, check out the [Azure documentation](https://docs.microsoft.com/azure/azure-functions/functions-triggers-bindings#supported-bindings).</span></span>

### <a name="a-sample-trigger-and-binding-functionjson"></a><span data-ttu-id="525d4-139">Beispieltrigger und -bindung (function.json)</span><span class="sxs-lookup"><span data-stu-id="525d4-139">A sample trigger and binding (function.json)</span></span>

<span data-ttu-id="525d4-140">Der folgende JSON-Code ist eine Beispieldefinition eines Triggers und einer Bindung für eine Funktion.</span><span class="sxs-lookup"><span data-stu-id="525d4-140">The following JSON is sample definition of a trigger and binding for a function.</span></span>

```javascript
{
  "bindings": [
    {
      "name": "order",
      "type": "queueTrigger",
      "direction": "in",
      "queueName": "myqueue-items",
      "connection": "MY_STORAGE_ACCT_APP_SETTING"
    },
    {
      "name": "$return",
      "type": "table",
      "direction": "out",
      "tableName": "outTable",
      "connection": "MY_TABLE_STORAGE_ACCT_APP_SETTING"
    }
  ]
}
```

<span data-ttu-id="525d4-141">Wie bereits erwähnt wurde, verfügt jede Bindung über eine Richtung, die definiert, ob es sich um eine Eingabe- oder eine Ausgabebindung handelt.</span><span class="sxs-lookup"><span data-stu-id="525d4-141">As we mentioned previously, each binding has a direction that defines whether it is an input or output binding.</span></span> <span data-ttu-id="525d4-142">Trigger sind stets Eingabebindungen.</span><span class="sxs-lookup"><span data-stu-id="525d4-142">Triggers are always input bindings.</span></span> <span data-ttu-id="525d4-143">Das obige Beispiel zeigt eine Funktion, die durch eine Nachricht ausgelöst wird, welche der Warteschlange namens **myqueue-items** hinzugefügt wird.</span><span class="sxs-lookup"><span data-stu-id="525d4-143">The preceding sample shows a function that is triggered by a message being added to a queue named **myqueue-items**.</span></span> <span data-ttu-id="525d4-144">Danach wird der Rückgabewert der Funktion an die Tabelle **outTable** im Azure-Tabellenspeicher gesendet.</span><span class="sxs-lookup"><span data-stu-id="525d4-144">It then sends the return value of the function to the **outTable** table in Azure Table storage.</span></span>

## <a name="creating-a-function-in-the-azure-portal"></a><span data-ttu-id="525d4-145">Erstellen einer Funktion im Azure-Portal</span><span class="sxs-lookup"><span data-stu-id="525d4-145">Creating a function in the Azure portal</span></span>

<span data-ttu-id="525d4-146">Azure stellt vorkonfigurierte Vorlagen für gängige Szenarios mit Azure-Funktionen bereit.</span><span class="sxs-lookup"><span data-stu-id="525d4-146">Azure provides pre-configured templates for common Azure function scenarios.</span></span>

### <a name="quickstart-templates"></a><span data-ttu-id="525d4-147">Schnellstartvorlagen</span><span class="sxs-lookup"><span data-stu-id="525d4-147">Quickstart templates</span></span>

<span data-ttu-id="525d4-148">Um eine Azure-Funktion hinzuzufügen, müssen Sie eine Funktions-App auswählen.</span><span class="sxs-lookup"><span data-stu-id="525d4-148">To add an Azure function, you must select a function app.</span></span> <span data-ttu-id="525d4-149">Sie erkennen eine Funktions-App am Blitzsymbol zwischen spitzen Klammern.</span><span class="sxs-lookup"><span data-stu-id="525d4-149">The function app can be identified by the lightning bolt within pointy brackets icon.</span></span>  
![Screenshot einer Funktions-App, die in einer Ressourcengruppe ausgewählt wurde.](../images/5-function-icon.png)

<span data-ttu-id="525d4-151">Wenn Sie zum ersten Mal eine Azure-Funktion hinzufügen, wird Ihnen der Bildschirm für den Schnellstart angezeigt.</span><span class="sxs-lookup"><span data-stu-id="525d4-151">When adding your first Azure function, you are presented with the Quickstart screen.</span></span> <span data-ttu-id="525d4-152">Auf diesem Bildschirm können Sie einen Triggertyp und eine Programmiersprache auswählen.</span><span class="sxs-lookup"><span data-stu-id="525d4-152">This screen allows you to choose a trigger type and programming language.</span></span> <span data-ttu-id="525d4-153">Azure erstellt dann basierend auf Ihrer Auswahl den Funktionscode und die Konfiguration für Sie.</span><span class="sxs-lookup"><span data-stu-id="525d4-153">Then, based on your selections, Azure will generate the function code and configuration for you.</span></span> 
 
![Schnellstart für Funktion](../images/5-quickstart-form.png)

### <a name="function-templates"></a><span data-ttu-id="525d4-155">Funktionsvorlagen</span><span class="sxs-lookup"><span data-stu-id="525d4-155">Function templates</span></span>

<span data-ttu-id="525d4-156">Die Auswahl von Vorlagen ist nicht auf die Vorlagen beschränkt, die im Schnellstart angezeigt werden.</span><span class="sxs-lookup"><span data-stu-id="525d4-156">The selection of templates is not limited to the templates displayed in the Quickstart.</span></span> <span data-ttu-id="525d4-157">Sie können aus über 30 verschiedenen Vorlagen auswählen.</span><span class="sxs-lookup"><span data-stu-id="525d4-157">You also have the option of choosing from over 30 different templates.</span></span> <span data-ttu-id="525d4-158">Sie können auf den Bildschirm mit der Vorlagenliste zugreifen, während Sie weitere Funktionen erstellen oder indem Sie auf dem Schnellstartbildschirm die Option **Benutzerdefinierte Funktion** auswählen.</span><span class="sxs-lookup"><span data-stu-id="525d4-158">You can access the template list screen while creating subsequent functions, or by selecting the **Custom function** option on the Quickstart screen.</span></span>  
<span data-ttu-id="525d4-159">![Screenshot der Azure Functions-Schnellstartbenutzeroberfläche, die Sie mit einer vorgefertigten Funktion bei den ersten Schritten unterstützt.](../images/5-template-list.png)</span><span class="sxs-lookup"><span data-stu-id="525d4-159">![Screenshot of Azure Functions Quickstart user interface to help you get started quickly with a premade function.](../images/5-template-list.png)</span></span>

## <a name="navigating-to-your-function-and-files"></a><span data-ttu-id="525d4-160">Navigieren zu Ihrer Funktion und Ihren Dateien</span><span class="sxs-lookup"><span data-stu-id="525d4-160">Navigating to your function and files</span></span>

<span data-ttu-id="525d4-161">Nachdem Sie eine Funktion anhand einer Vorlage erstellt haben, werden mehrere Dateien generiert.</span><span class="sxs-lookup"><span data-stu-id="525d4-161">When you create a function from a template, several files are created.</span></span> <span data-ttu-id="525d4-162">Wenn Sie beispielsweise den Schnellstart „Webhook + API“ mit JavaScript gewählt haben, handelt es sich bei den generierten Dateien um eine Konfigurationsdatei, **function.json**, und eine Quellcodedatei, **index.js**.</span><span class="sxs-lookup"><span data-stu-id="525d4-162">For example, if you opted to use the Webhook + API Quickstart using JavaScript, the files generated would be a configuration file, **function.json**, and a source code file, **index.js**.</span></span> <span data-ttu-id="525d4-163">Die Funktionen, die Sie in einer Funktions-App erstellen, werden im Portal für Funktions-Apps unter dem Menüelement „Funktionen“ angezeigt.</span><span class="sxs-lookup"><span data-stu-id="525d4-163">The functions you create in a function app appear under the Functions menu item in the function app portal.</span></span>

<span data-ttu-id="525d4-164">Wenn Sie eine Funktion in Ihrer Funktions-App auswählen, wird ein Code-Editor geöffnet, in dem – wie im folgenden Screenshot – der Code für Ihre Funktion angezeigt wird.</span><span class="sxs-lookup"><span data-stu-id="525d4-164">When you select a function in your function app, a code editor opens and displays the code for your function, as illustrated in the following screenshot.</span></span>

![Screenshot der Benutzeroberfläche zur Auswahl von Funktionsvorlagen mit einer Liste von Vorlagen, mit denen Sie die Funktionsentwicklung beschleunigen können.](../images/5-file-navigation.png)

<span data-ttu-id="525d4-166">Wie Sie im obigen Screenshot sehen können, gibt es auf der rechten Seite ein Flyoutmenü, das die Registerkarte **Dateien anzeigen** enthält.</span><span class="sxs-lookup"><span data-stu-id="525d4-166">As you can see in the preceding screenshot,  there's a flyout menu on the right that includes a tab to **View files**.</span></span> <span data-ttu-id="525d4-167">Wenn Sie diese Registerkarte auswählen, wird die Dateistruktur angezeigt, aus der sich Ihre Funktion zusammensetzt.</span><span class="sxs-lookup"><span data-stu-id="525d4-167">Selecting this tab shows the file structure that makes up your function.</span></span>  

## <a name="execution-options-for-testing-your-azure-function"></a><span data-ttu-id="525d4-168">Ausführungsoptionen zum Testen Ihrer Azure-Funktion</span><span class="sxs-lookup"><span data-stu-id="525d4-168">Execution options for testing your Azure function</span></span>

<span data-ttu-id="525d4-169">Nachdem Sie eine Funktion erstellt haben, sollten Sie diese testen.</span><span class="sxs-lookup"><span data-stu-id="525d4-169">Once you've created a function, you'll want to test it.</span></span> <span data-ttu-id="525d4-170">Hierzu gibt es verschiedene Verfahren: manuelles Ausführen und Testen im Azure-Portal.</span><span class="sxs-lookup"><span data-stu-id="525d4-170">There are a couple of approaches: manual execution and testing from within the Azure portal itself.</span></span>

### <a name="manual-execution-of-a-function"></a><span data-ttu-id="525d4-171">Manuelle Ausführung einer Funktion</span><span class="sxs-lookup"><span data-stu-id="525d4-171">Manual execution of a function</span></span>

<span data-ttu-id="525d4-172">Sie können eine Funktion starten, indem Sie den konfigurierten Trigger manuell auslösen.</span><span class="sxs-lookup"><span data-stu-id="525d4-172">You can start a function by manually triggering the configured trigger.</span></span> <span data-ttu-id="525d4-173">Wenn Sie beispielsweise einen HttpTrigger verwenden, können Sie mit einem Tool wie Postman oder cURL eine HTTP-Anforderung an Ihrer Funktionsendpunkt-URL initiieren.</span><span class="sxs-lookup"><span data-stu-id="525d4-173">For instance, if you are using an HttpTrigger - you can use a tool such as Postman or cURL to initiate an HTTP request to your function endpoint URL.</span></span>  

![<span data-ttu-id="525d4-174">Teil-Screenshot der Postman-Anwendungsschnittstelle mit einer im Feld „URL“ eingegebenen Funktions-URL und Beispieltext im Anforderungstextbereich.</span><span class="sxs-lookup"><span data-stu-id="525d4-174">Partial screenshot of the Postman application interface with a function URL typed into the url field and the request body filled out with a sample body.</span></span> ](../images/5-postman-execution.png)

<span data-ttu-id="525d4-175">Um die Endpunkt-URL zu erhalten, wählen Sie den HTTP-Trigger im linken Navigationsbereich aus, und wählen Sie dann die Schaltfläche **Funktions-URL abrufen** aus.</span><span class="sxs-lookup"><span data-stu-id="525d4-175">To get endpoint URL, select the HTTP Trigger from the left navigation, and then select the **Get function URL** button.</span></span>  

![Screenshot des Funktions-Apps-Portals mit einer ausgewählten Funktion und der am oberen Rand der Seite ausgewählten Aktion *Funktions-URL abrufen*.](../images/5-get-function-url.png)

### <a name="testing-a-function-in-the-azure-portal"></a><span data-ttu-id="525d4-177">Testen einer Funktion im Azure-Portal</span><span class="sxs-lookup"><span data-stu-id="525d4-177">Testing a function in the Azure portal</span></span>

<span data-ttu-id="525d4-178">Im Azure-Portal lassen sich auch Ihre Funktionen bequem testen.</span><span class="sxs-lookup"><span data-stu-id="525d4-178">The portal also provides a convenient way to test your functions.</span></span> <span data-ttu-id="525d4-179">Auf der rechten Seite des Codefensters finden Sie ein Flyoutmenü mit Registerkartennavigation.</span><span class="sxs-lookup"><span data-stu-id="525d4-179">On the right side of the code window, there is a flyout tabbed navigation menu.</span></span> <span data-ttu-id="525d4-180">Dieses Menü enthält ein Element **Test**.</span><span class="sxs-lookup"><span data-stu-id="525d4-180">This menu contains a **Test** item.</span></span> <span data-ttu-id="525d4-181">Das Erweitern des Menüs und das Auswählen dieser Registerkarte ist eine weitere Möglichkeit, Ihre Funktion auszuführen und das Ergebnis anzuzeigen.</span><span class="sxs-lookup"><span data-stu-id="525d4-181">Expanding the menu and selecting this tab gives you another way to execute your function and view the result.</span></span> <span data-ttu-id="525d4-182">Wie im HttpTrigger-Szenario können Sie die HTTP-Methode festlegen und der Anforderung Parameter für die Abfragezeichenfolge sowie HTTP-Header hinzufügen.</span><span class="sxs-lookup"><span data-stu-id="525d4-182">In keeping with the HttpTrigger scenario, you can set the HTTP method, and add query string parameters and HTTP headers to the request.</span></span> <span data-ttu-id="525d4-183">Sie können ferner den Anforderungstext ändern, um weitere Szenarios zu testen.</span><span class="sxs-lookup"><span data-stu-id="525d4-183">You may also change the request body to test additional scenarios.</span></span> <span data-ttu-id="525d4-184">Im folgenden Beispiel ist der Test als HTTP POST-Anforderung mit einem Anforderungstext konfiguriert, der einen Parameter namens *Name* umfasst.</span><span class="sxs-lookup"><span data-stu-id="525d4-184">In the following example, the test is configured as an HTTP POST request with a request body that has one parameter called *name*.</span></span>
 
![Screenshot des Testfensters im Funktions-Apps-Portal mit einer HTTP POST-Anforderung und einem Beispielanforderungstext.](../images/5-portal-execution.png)

<span data-ttu-id="525d4-187">Wenn Sie in diesem Testfenster auf **Ausführen** klicken, werden die Ergebnisse zusammen mit einem Statuscode im Ausgabefenster angezeigt.</span><span class="sxs-lookup"><span data-stu-id="525d4-187">When you click **Run** in this test window, the results are displayed in the output window, along with a status code.</span></span> 

## <a name="monitoring-an-azure-function"></a><span data-ttu-id="525d4-188">Überwachen einer Azure-Funktion</span><span class="sxs-lookup"><span data-stu-id="525d4-188">Monitoring an Azure function</span></span>

<span data-ttu-id="525d4-189">Die Fähigkeit, Meldungen zu protokollieren und Ihre Funktionen zu überwachen, ist während der Entwicklung und in der Produktion unerlässlich.</span><span class="sxs-lookup"><span data-stu-id="525d4-189">The ability to log messages and monitor your functions is critical during development and in production.</span></span> <span data-ttu-id="525d4-190">Im Azure-Portal finden Sie ein Überwachungsdashboard und die Möglichkeit, Ausführungsprotokolle sowie Ausnahmen zu überprüfen, die von Ihren Azure-Funktionen empfangen wurden.</span><span class="sxs-lookup"><span data-stu-id="525d4-190">The Azure portal provides a monitoring dashboard as well as a way to review execution logs and exceptions obtained from your Azure functions.</span></span>

### <a name="log-window"></a><span data-ttu-id="525d4-191">Protokollfenster</span><span class="sxs-lookup"><span data-stu-id="525d4-191">Log window</span></span>

<span data-ttu-id="525d4-192">Sie können Ihrer Funktion auch Protokollanweisungen hinzufügen.</span><span class="sxs-lookup"><span data-stu-id="525d4-192">You're also able to add log statements to your function.</span></span> <span data-ttu-id="525d4-193">Diese Anweisungen werden im Protokollfenster der Funktion angezeigt.</span><span class="sxs-lookup"><span data-stu-id="525d4-193">These statements will appear in the log window of the function.</span></span> <span data-ttu-id="525d4-194">Sie finden das Protokollfenster in einem Flyoutmenü mit Registerkarten, das sich am unteren Rand des Codefensters befindet.</span><span class="sxs-lookup"><span data-stu-id="525d4-194">The log window is located in a tabbed flyout menu located at the bottom of the code window.</span></span> <span data-ttu-id="525d4-195">Der folgende JavaScript-Codeausschnitt zeigt, wie eine Meldung mit der `context.log`-Methode protokolliert wird.</span><span class="sxs-lookup"><span data-stu-id="525d4-195">The following JavaScript code snippet shows how to log a message using the `context.log` method.</span></span>

```javascript
  context.log('Enter your logging statement here');
```  

![Screenshot des Code-Editor-Abschnitts der Azure Functions-Benutzeroberfläche.](../images/5-log-window.png)

### <a name="errors-and-warnings-window"></a><span data-ttu-id="525d4-198">Fenster mit Fehlern und Warnungen</span><span class="sxs-lookup"><span data-stu-id="525d4-198">Errors and warnings window</span></span>

<span data-ttu-id="525d4-199">Sie finden die Registerkarte für das Fenster mit Fehlern und Warnungen im gleichen Flyoutmenü wie das Protokollfenster.</span><span class="sxs-lookup"><span data-stu-id="525d4-199">You can locate the errors and warnings window tab in the same flyout menu as the log window.</span></span> <span data-ttu-id="525d4-200">Dieser Fehler zeigt Kompilierungsfehler und -warnungen innerhalb Ihres Codes.</span><span class="sxs-lookup"><span data-stu-id="525d4-200">This window will show compilation errors and warnings within your code.</span></span> <span data-ttu-id="525d4-201">Da JavaScript eine dynamische und interpretierte Sprache ist, zeigt die folgende Abbildung einen Kompilierungsfehler in einer C#-Funktion.</span><span class="sxs-lookup"><span data-stu-id="525d4-201">As JavaScript is a dynamic and interpreted language, the following image shows a compilation error in a C# function.</span></span>  

![Screenshot des Code-Editor-Abschnitts der Azure Functions-Benutzeroberfläche, bei dem das Fenster **Protokolle** am unteren Bildschirmrand hervorgehoben ist.](../images/5-errors-window.png)

### <a name="monitoring-dashboard"></a><span data-ttu-id="525d4-203">Überwachungsdashboard</span><span class="sxs-lookup"><span data-stu-id="525d4-203">Monitoring dashboard</span></span>

<span data-ttu-id="525d4-204">Wenn Sie im Navigationsmenü der Funktions-App den Funktionsknoten erweitern, sehen Sie ein Menüelement **Monitor**.</span><span class="sxs-lookup"><span data-stu-id="525d4-204">In the function app navigation menu, once you expand the function node - you will see a **Monitor** menu item.</span></span> <span data-ttu-id="525d4-205">Das Überwachungsdashboard bietet eine schnelle Möglichkeit, den Verlauf von Funktionsausführungen anzuzeigen.</span><span class="sxs-lookup"><span data-stu-id="525d4-205">The monitor dashboard provides a quick way to view the history of function executions.</span></span> <span data-ttu-id="525d4-206">Diese Ansicht zeigt den Zeitstempel, den Ergebniscode, die Dauer und die Vorgangs-ID und zeigt ebenfalls an, ob die Ausführung erfolgreich abgeschlossen wurde.</span><span class="sxs-lookup"><span data-stu-id="525d4-206">This view displays the timestamp, result code, duration, and operation ID, as well as if it completed successfully.</span></span> <span data-ttu-id="525d4-207">Die Daten werden über Azure Application Insights aufgefüllt.</span><span class="sxs-lookup"><span data-stu-id="525d4-207">The data is populated via Azure Application Insights.</span></span>  

![Screenshot des Überwachungsdashboards, das über den Funktionsmenüpunkt **Monitor** gestartet wurde, einschließlich einer Liste der erfolgreichen und fehlgeschlagenen Aufrufe der Funktion.](../images/5-monitor-function.png)
